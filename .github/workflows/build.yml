name: Build EnglishLearningApp

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ created ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install aqtinstall
      run: |
        python -m pip install setuptools>=70.1.0 py7zr==0.22.* aqtinstall==3.2.*
      shell: cmd

    - name: Download Qt Installer
      run: |
        $url = "https://mirror.netcologne.de/qtproject/archive/online_installers/4.7/qt-unified-windows-x64-4.7.0-online.exe"
        $dest = "$env:TEMP\qt-installer.exe"
        Invoke-WebRequest -Uri $url -OutFile $dest
      shell: powershell

    - name: Create Qt Installer Script
      run: |
        $script = @"
        function Controller() {
            installer.setMessageBoxAutoAnswer("licenseAgreement", "Yes");
            installer.autoAcceptMessageBoxes();
            installer.setDefaultPageVisible(QInstaller.TargetDirectory, false);
            installer.setDefaultPageVisible(QInstaller.ComponentSelection, false);
            installer.setDefaultPageVisible(QInstaller.LicenseCheck, false);
            installer.setDefaultPageVisible(QInstaller.StartMenuSelection, false);
            installer.setDefaultPageVisible(QInstaller.ReadyForInstallation, false);
            installer.setDefaultPageVisible(QInstaller.PerformInstallation, false);
            installer.setDefaultPageVisible(QInstaller.InstallationFinished, false);
            installer.setDefaultPageVisible(QInstaller.FinishedPage, false);
        }
        Controller.prototype.WelcomePageCallback = function() {
            gui.clickButton(buttons.NextButton);
        }
        Controller.prototype.CredentialsPageCallback = function() {
            var widget = gui.currentPageWidget();
            widget.setUsername("${{ secrets.QT_EMAIL }}");
            widget.setPassword("${{ secrets.QT_PW }}");
            gui.clickButton(buttons.NextButton);
        }
        Controller.prototype.ComponentSelectionPageCallback = function() {
            var widget = gui.currentPageWidget();
            widget.selectComponent("qt.qt6.682.win64_msvc2022_64");
            widget.selectComponent("qt.qt6.682.qtmultimedia");
            widget.selectComponent("qt.qt6.682.qttexttospeech");
            gui.clickButton(buttons.NextButton);
        }
        Controller.prototype.TargetDirectoryPageCallback = function() {
            var widget = gui.currentPageWidget();
            widget.TargetDirectoryLineEdit.setText("${{ runner.temp }}/qt");
            gui.clickButton(buttons.NextButton);
        }
        Controller.prototype.FinishedPageCallback = function() {
            gui.clickButton(buttons.FinishButton);
        }
        "@
        Set-Content -Path "$env:TEMP\qt-installer-script.qs" -Value $script
      shell: powershell

    - name: Run Qt Installer
      run: |
        Start-Process -FilePath "$env:TEMP\qt-installer.exe" -ArgumentList "--script", "$env:TEMP\qt-installer-script.qs" -Wait
      shell: powershell

    - name: Locate Qt6Config.cmake
      id: qt_path
      run: |
        $file = Get-ChildItem -Recurse -Path "${{ runner.temp }}\qt" -Filter "Qt6Config.cmake" -ErrorAction SilentlyContinue | Select-Object -First 1
        if ($null -eq $file) {
          Write-Error "Qt6Config.cmake not found"
          exit 1
        }
        $dir = $file.DirectoryName
        echo "QT_PREFIX=$dir" >> $env:GITHUB_OUTPUT
      shell: powershell

    - name: Set up MSVC
      uses: ilammy/msvc-dev-cmd@v1

    - name: Create build directory
      run: mkdir build
      shell: cmd

    - name: Configure CMake
      working-directory: build
      run: |
        cmake -S .. -B . -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH="${{ steps.qt_path.outputs.QT_PREFIX }}" -DQT_DEBUG_FIND_PACKAGE=ON
      shell: cmd

    - name: Debug Qt6TextToSpeech
      if: failure()
      working-directory: build
      run: |
        cmake -S .. -B . --debug-find-pkg=Qt6TextToSpeech
      shell: cmd

    - name: Build
      working-directory: build
      run: cmake --build . --config Release
      shell: cmd

    - name: Deploy dependencies with windeployqt
      working-directory: build/Release
      run: "${{ steps.qt_path.outputs.QT_PREFIX }}/../../../bin/windeployqt --release EnglishLearningApp.exe"
      shell: cmd

    - name: Set version variable
      id: version
      run: |
        if ("${{ github.event_name }}" -eq "release") {
          echo "VERSION=${{ github.event.release.tag_name }}" >> $env:GITHUB_OUTPUT
        } else {
          echo "VERSION=latest" >> $env:GITHUB_OUTPUT
        }
      shell: powershell

    - name: Create ZIP
      working-directory: build/Release
      run: |
        $zipName = "EnglishLearningApp_v${{ steps.version.outputs.VERSION }}.zip"
        Compress-Archive -Path ./* -DestinationPath $zipName -CompressionLevel Optimal
      shell: powershell

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: EnglishLearningApp_v${{ steps.version.outputs.VERSION }}
        path: build/Release/EnglishLearningApp_v${{ steps.version.outputs.VERSION }}.zip

    - name: Upload to Release
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v2
      with:
        files: build/Release/EnglishLearningApp_v${{ steps.version.outputs.VERSION }}.zip
